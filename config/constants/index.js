/**
 * Makes all constants available through a single require.
 */

module.exports = { 
	appDefaults : require('./app-defaults'),
	appContants : require('./app-constants'),
	responseMessages : require('./response-messages')
};